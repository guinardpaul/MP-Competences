import { SortByRefCtPipe } from './sort-by-ref-ct.pipe';

describe('SortByRefCtPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByRefCtPipe();
    expect(pipe).toBeTruthy();
  });
});
