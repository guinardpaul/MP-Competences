import { SortByStringPipe } from './sort-by-string.pipe';

describe('SortByStringPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByStringPipe();
    expect(pipe).toBeTruthy();
  });
});
